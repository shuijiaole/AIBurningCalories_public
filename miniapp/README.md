# Miniapp

这个目录已经收拢了小程序前端相关内容，后续可以直接单独拷走：

- `src/` 小程序源码
- `config/` Taro 构建配置
- `public/` 静态文件
- `dist/` 当前编译产物
- `package.json` / `package-lock.json` 前端依赖与脚本
- `project.config.json` 微信开发者工具项目配置

常用命令：

```bash
npm install
npm run dev:weapp
npm run build:weapp
```
